name: Hello 

on:
  push:
    branches: [main]


jobs:

  greet:

    runs-on: ubuntu-latest
    steps:

      - name: Say hello to user
        run: echo "Hello Friends"

      - name: Say hello to user
        run: echo "Hello $USER"


